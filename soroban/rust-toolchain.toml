# Soroban contract builds require Rust >= 1.84 for the wasm32v1-none target
# used by stellar-cli 22.x. soroban-sdk 22.0.11 declares the same MSRV.
[toolchain]
channel = "1.84.0"
targets = [
    "wasm32-unknown-unknown",
    "wasm32v1-none",
]
