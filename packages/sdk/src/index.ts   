/**
 * @oversync/sdk
 * ============
 * Public API surface for the OverSync cross-chain swap SDK.
 *
 * Deployment evidence helpers are re-exported here so SCF / investor
 * tooling can import directly from the package root:
 *
 * ```ts
 * import { getDeploymentEvidence } from "@oversync/sdk";
 * ```
 */

// ── Deployment evidence (read-only, no secrets) ──────────────────────────────
export {
  getDeploymentEvidence,
  getEvidenceByContract,
  getEvidenceByChain,
  getEvidenceGeneratedAt,
} from "./deployment-evidence";

export type { DeploymentEvidence, DeploymentStatus } from "./deployment-evidence";